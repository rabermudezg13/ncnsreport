import { AlertTriangle,CalendarDays,Clock3,Users } from "lucide-react";import { format,isSameDay,isSameMonth,isSameWeek } from "date-fns";import { Bar,BarChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis } from "recharts";import { Card,Empty,PageHeader } from "@/components/ui";import { useData } from "@/hooks/useData";import { listIncidents,listSubstitutes } from "@/services/data";import { incidentLabels } from "@/types";
export function DashboardPage(){const{data,loading,error}=useData(async()=>{const[i,s]=await Promise.all([listIncidents(),listSubstitutes()]);return{incidents:i,subs:s}},[]);if(loading)return <p className="text-muted">Loading dashboard…</p>;if(error||!data)return <Empty title="Dashboard unavailable" body={error}/>;const now=new Date();const month=data.incidents.filter(i=>isSameMonth(i.incidentDate.toDate(),now));const stats=[{label:"Incidents Today",value:data.incidents.filter(i=>isSameDay(i.incidentDate.toDate(),now)).length,icon:Clock3},{label:"This Week",value:data.incidents.filter(i=>isSameWeek(i.incidentDate.toDate(),now)).length,icon:CalendarDays},{label:"This Month",value:month.length,icon:AlertTriangle},{label:"Repeat Offenders",value:data.subs.filter(s=>s.totalIncidents>=2).length,icon:Users}];const byMonth=Array.from({length:6},(_,n)=>{const d=new Date(now.getFullYear(),now.getMonth()-5+n,1);return{name:format(d,"MMM"),incidents:data.incidents.filter(i=>isSameMonth(i.incidentDate.toDate(),d)).length}});return <><PageHeader title="Dashboard" description="A current view of substitute attendance incidents."/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(({label,value,icon:Icon})=><Card key={label} className="p-5"><div className="flex items-start justify-between"><div><p className="text-sm text-muted">{label}</p><p className="mt-2 text-3xl font-bold">{value}</p></div><span className="rounded-lg bg-blue-50 p-2 text-primary"><Icon size={20}/></span></div></Card>)}</div><div className="mt-6 grid gap-6 xl:grid-cols-[1.5fr_1fr]"><Card className="p-5"><h2 className="font-semibold">Incidents by month</h2><div className="mt-5 h-64"><ResponsiveContainer width="100%" height="100%"><BarChart data={byMonth}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="name"/><YAxis allowDecimals={false}/><Tooltip/><Bar dataKey="incidents" fill="#2563eb" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></div></Card><Card className="p-5"><h2 className="font-semibold">Recent activity</h2><div className="mt-4 divide-y">{data.incidents.slice(0,6).map(i=><div className="py-3" key={i.id}><div className="flex justify-between gap-3"><p className="text-sm font-semibold">{i.substituteName}</p><p className="text-xs text-muted">{format(i.incidentDate.toDate(),"MMM d")}</p></div><p className="mt-1 text-xs text-muted">{i.schoolName} · {incidentLabels[i.incidentType]}</p></div>)}{!data.incidents.length?<p className="py-12 text-center text-sm text-muted">No incident activity yet.</p>:null}</div></Card></div></>}
