import { useState } from "react";import { Navigate } from "react-router-dom";import { ShieldCheck } from "lucide-react";import { Button,Card,Field,Input } from "@/components/ui";import { firebaseConfigured } from "@/lib/firebase";import { useAuth } from "./AuthContext";
export function LoginPage(){const{user,profile,login}=useAuth();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);if(user&&profile?.active)return <Navigate to="/" replace/>;async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{await login(email,password)}catch{setError("Unable to sign in. Check your email and password.")}finally{setBusy(false)}}return <main className="grid min-h-screen lg:grid-cols-2"><section className="hidden bg-brand p-12 text-white lg:flex lg:flex-col lg:justify-between"><div className="flex items-center gap-3 text-lg font-bold"><span className="grid size-10 place-items-center rounded-xl bg-white/10"><ShieldCheck/></span>NCNS Report</div><div className="max-w-xl"><p className="text-sm font-semibold uppercase tracking-[.25em] text-blue-200">Attendance intelligence</p><h1 className="mt-5 text-5xl font-bold leading-tight">Clear records.<br/>Better follow-up.</h1><p className="mt-6 text-lg leading-8 text-blue-100">A secure workspace for reporting and reviewing substitute attendance incidents.</p></div><p className="text-sm text-blue-200">Created by Rodrigo Bermuddez with <span className="text-rose-300" role="img" aria-label="love">♥</span></p></section><section className="flex min-h-screen flex-col items-center justify-center bg-slate-50 p-6"><Card className="w-full max-w-md p-8"><div className="mb-8 lg:hidden"><span className="font-bold text-brand">NCNS Report</span></div><h2 className="text-2xl font-bold">Welcome back</h2><p className="mt-2 text-sm text-muted">Sign in with your organization email.</p>{!firebaseConfigured?<div className="mt-5 rounded-lg bg-amber-50 p-3 text-sm text-amber-800">Firebase environment variables are required before sign-in.</div>:null}<form className="mt-7 grid gap-5" onSubmit={submit}><Field label="Email"><Input type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/></Field><Field label="Password"><Input type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)}/></Field>{error?<p role="alert" className="text-sm text-danger">{error}</p>:null}<Button disabled={busy||!firebaseConfigured}>{busy?"Signing in…":"Sign in"}</Button></form></Card><p className="mt-6 text-xs text-muted lg:hidden">Created by Rodrigo Bermuddez with <span className="text-rose-500" role="img" aria-label="love">♥</span></p></section></main>}
