import type { Incident } from "@/types";import { incidentLabels } from "@/types";
const esc=(v:unknown)=>'"'+String(v??"").replaceAll('"','""')+'"';
export function downloadCSV(rows:Incident[]){const header=["Date","Substitute","BH","KSN","School","Scheduled Time","Incident Type","Response","Created By"];const body=rows.map(i=>[i.incidentDate.toDate().toISOString().slice(0,10),i.substituteName,i.bhNumber,i.ksnNumber,i.schoolName,i.scheduledTime,incidentLabels[i.incidentType],i.responseStatus,i.createdByName]);const csv=[header,...body].map(r=>r.map(esc).join(",")).join("\n");const url=URL.createObjectURL(new Blob([csv],{type:"text/csv"}));const a=document.createElement("a");a.href=url;a.download="ncns-report-"+new Date().toISOString().slice(0,10)+".csv";a.click();URL.revokeObjectURL(url)}
